// @flow
import * as React from 'react';

type Props = {

};

export function AboutPage(props: Props) {
    return (
        <div>
            Hello
        </div>
    );
};

export default React.memo(AboutPage);